# Sitio estático, sin paso de build. Se publica la raíz del repo tal cual.
[build]
  publish = "."
  command = ""

# Cabeceras básicas
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
